package org.ird.unfepi;

import org.junit.Test;

public class ReminderSmsServiceTest {

	@Test
	public void reminderSmsTest(){
			
		/*ServiceContext sc = Context.getServices();
		Vaccination nextvacc = sc.getVaccinationService().getVaccinationRecord(415, false, null, null);
		List<ReminderSms> remindersmses = IMRUtils.createReminderSms(nextvacc, Globals.DEFAULT_ARM, null);
		
		for (ReminderSms reminderSms : remindersmses) {
			sc.getReminderService().addReminderSmsRecord(reminderSms);
		}
		sc.commitTransaction();*/
	}
}